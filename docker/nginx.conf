server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        autoindex on;
    }

    location /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}
